apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# we absolutely need korifi cloned as a sibling to korifi-ci
resources:
- ../../../../../korifi/job-task-runner/config/default

configMapGenerator:
- behavior: merge
  files:
  - job_task_runner_config.yaml
  name: config
