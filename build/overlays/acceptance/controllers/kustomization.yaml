configMapGenerator:
- behavior: merge
  files:
  - controllersconfig/korifi_controllers_config.yaml
  name: config

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# we absolutely need korifi cloned as a sibling to korifi-ci
resources:
- ../../../../../korifi/controllers/config/default
