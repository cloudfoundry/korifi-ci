platform: linux
inputs:
- name: korifi-ci
- name: cf-k8s-secrets
params:
  CLUSTER_NAME:
  GCP_SERVICE_ACCOUNT_JSON: ((gcp-serviceaccount))
  WORKER_COUNT: 3
run:
  path: korifi-ci/pipelines/scripts/delete-cluster.sh
